; xin standard library

; boolean aliases
(: true 1)
(: false 0)

; basic io
(: (log s)
   (<- os::stdout (+ (string s) '\n')))
(: (scan)
   (-> os::stdin))

; iteration primitives: range, times

; vec functions

; set abstraction over map

; string manipulations
